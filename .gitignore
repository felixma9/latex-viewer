# LaTeX build artifacts
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.lof
*.lot
*.bbl
*.blg
*.nav
*.snm
*.vrb
*.xdv
*.run.xml
*.bcf
.build/

# Local deployment settings (see .env.example)
.env
__pycache__/
.pytest_cache/

# Editor / OS
*.swp
.DS_Store

# Local Claude settings
.claude/settings.local.json

# Your documents are personal: never commit them. The empty folder is kept so
# the bind mount is created with your ownership rather than root's.
documents/*
!documents/.gitkeep

# SDD scratch workspace (subagent-driven development)
.superpowers/
