# LaTeX build artifacts
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.lof
*.lot
*.bbl
*.blg
*.nav
*.snm
*.vrb
*.xdv
*.run.xml
*.bcf
.build/

# Local deployment settings (see .env.example)
.env
__pycache__/
.pytest_cache/

# Editor / OS
*.swp
.DS_Store

# Local Claude settings
.claude/settings.local.json

# Your documents are personal: never commit them. The empty folder is kept so
# the bind mount is created with your ownership rather than root's.
documents/*
!documents/.gitkeep
# Exception (this fork): the master resume source is tracked and published to
# felixma9.github.io by .github/workflows/publish-resume.yml. Only main.tex.
!documents/resume-general/
documents/resume-general/*
!documents/resume-general/main.tex

# SDD scratch workspace (subagent-driven development)
.superpowers/
